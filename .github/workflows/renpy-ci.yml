name: Ren'Py CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache Ren'Py
        id: cache-renpy
        uses: actions/cache@v4
        with:
          path: renpy
          key: ${{ runner.os }}-renpy-${{ hashFiles('**/.renpy-version') || '8.3' }}

      - name: Setup Ren'Py
        uses: Ayowel/renpy-setup-action@v2.0.1
        if: steps.cache-renpy.outputs.cache-hit != 'true'
        with:
          action: install
          install_dir: renpy

      - name: Run Ren'Py Lint (with disabled testcases)
        uses: Ayowel/renpy-setup-action@v2.0.1
        with:
          action: lint
          install_dir: renpy
          game: .
          extra_args: "--config config.testcases=False"